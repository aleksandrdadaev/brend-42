import { FC } from 'react'

import { CreateNominationForm } from '@/widgets/create-nomination-form'

export const CreateNomination: FC = () => {
	return (
		<main>
			<CreateNominationForm />
		</main>
	)
}
